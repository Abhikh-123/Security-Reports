apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: security-reports
  namespace: default
  description: Security Reports – Code Scoring & Scan Reports Platform
  tags:
    - security
    - reports
    - devsecops
  annotations:
    # Location annotations (IMPORTANT)
    backstage.io/managed-by-location: url:https://raw.githubusercontent.com/Abhikh-123/Security-Reports/main/catalog-info.yaml
    backstage.io/source-location: url:https://github.com/Abhikh-123/Security-Reports
    
    # GitHub integration
    github.com/project-slug: Abhikh-123/Security-Reports
    
    # Jenkins integration
    jenkins.io/job-full-name: CodeScoring
    jenkins.io/job-name: CodeScoring
    
    # TechDocs
    backstage.io/techdocs-ref: dir:.
    
    # Optional but helpful
    backstage.io/kubernetes-id: security-reports
    
spec:
  type: service
  lifecycle: production
  owner: group:default/security-team
  # Optional: Add system if you have one
  # system: security-platform
